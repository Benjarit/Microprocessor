OUTPUT	MACRO MESSAGE
        PUSH DX
	LEA DX, MESSAGE
	MOV AH, 09H
	INT 21H
	POP DX
	ENDM


INPUT	MACRO BUFFER
    	PUSH DX
    	PUSH AX
		LEA DX, BUFFER
		MOV AH, 0AH
		INT 21H
		POP AX
		POP DX
		ENDM
