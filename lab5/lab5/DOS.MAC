OUTC MACRO CHAR
	PUSH AX
	PUSH DX
	MOV DL, [CHAR]
	MOV AH, 02H
	INT 21H
	POP DX
	POP AX
ENDM
OUTSTRING MACRO MSG
	PUSH AX
	PUSH DX
	MOV DX, OFFSET MSG
	MOV AH, 09H
	INT 21H
	POP DX
	POP AX
ENDM

